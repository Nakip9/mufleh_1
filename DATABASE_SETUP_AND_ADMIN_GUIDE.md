# دليل إعداد قاعدة البيانات وإدارة المعرض والعروض

لتمكين إدارة "العروض والمعرض" من لوحة التحكم، يجب عليك إعداد قاعدة البيانات (Supabase) وربطها بالمشروع.

## الخطوة 1: إعداد قاعدة البيانات في Supabase

1.  قم بتسجيل الدخول إلى [Supabase](https://supabase.com).
2.  أنشئ مشروعاً جديداً (New Project).
3.  بعد إنشاء المشروع، اذهب إلى **SQL Editor** من القائمة الجانبية.
4.  اضغط على **New Query** والصق الكود التالي لإنشاء جدول العروض والمعرض:

```sql
-- إنشاء جدول العروض والمعرض
CREATE TABLE offers_gallery (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT,
  type TEXT CHECK (type IN ('offer', 'gallery')), -- يحدد النوع: عرض أو صورة معرض
  image_url TEXT,
  price TEXT, -- اختياري، للعروض فقط
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW())
);

-- تفعيل سياسات الأمان (Row Level Security) - اختياري ولكن موصى به
ALTER TABLE offers_gallery ENABLE ROW LEVEL SECURITY;

-- السماح بالقراءة للجميع (Public Read)
CREATE POLICY "Enable read access for all users" ON offers_gallery FOR SELECT USING (true);

-- السماح بالإظافة والتعديل والحذف فقط (يمكنك تقييدها لاحقاً، حالياً نسمح للجميع لسهولة الربط مع API)
CREATE POLICY "Enable insert for all users" ON offers_gallery FOR INSERT WITH CHECK (true);
CREATE POLICY "Enable update for all users" ON offers_gallery FOR UPDATE USING (true);
CREATE POLICY "Enable delete for all users" ON offers_gallery FOR DELETE USING (true);
```

5.  اضغط **Run** لتنفيذ الكود.

## الخطوة 2: الحصول على مفاتيح الربط (API Keys)

1.  اذهب إلى **Project Settings** (أيقونة الترس).
2.  اختر **API**.
3.  ستجد `Project URL` و `anon` / `public` key.
4.  ستحتاج أيضاً إلى `service_role` key (سري للغاية) لاستخدامه في السيرفر (Vercel).

## الخطوة 3: ربط المشروع بالمفاتيح (Environment Variables)

**محلياً (على جهازك):**
1.  أنشئ ملفاً باسم `.env` في المجلد الرئيسي للمشروع (بجانب `package.json`).
2.  أضف القيم التالية:

```env
SUPABASE_URL=رابط_مشروعك_هنا
SUPABASE_SERVICE_KEY=مفتاح_service_role_هنا
```

**على Vercel (عند الرفع):**
1.  اذهب إلى إعدادات المشروع على Vercel.
2.  اختر **Environment Variables**.
3.  أضف نفس المتغيرات (`SUPABASE_URL` و `SUPABASE_SERVICE_KEY`).

---

## الخطوة 4: إدارة الصور (اختياري ولكن مهم)

لتتمكن من رفع الصور مباشرة، تحتاج لإنشاء **Storage Bucket**:
1.  اذهب إلى **Storage** في Supabase.
2.  أنشئ Bucket جديد باسم `images`.
3.  اجعله **Public**.
4.  الآن يمكنك رفع الصور إليه والحصول على روابط مباشرة (`Get Public URL`).

---

## تم الإعداد!
الآن ستقوم لوحة التحكم في موقعك بقراءة وكتابة البيانات مباشرة من هذا الجدول.
